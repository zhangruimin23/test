# 科研公共数据源采集与集成 CLI 解决方案

## 目标与范围

选择 PubChem PUG REST API，交付可运行、可演示、可审计的最小闭环：

```text
PubChem API -> 原始 JSON 留存 -> 规范化 JSONL -> SQLite 幂等同步 -> 质量报告
```

重点是数据工程可靠性，而非一次覆盖所有科研数据源。未来可通过统一 `SourceAdapter` 扩展 UniProt、PubMed。

项目结构如下：

```text
dataeng-cli-demo/
  dataeng_cli.py       fetch、sync、validate 的 CLI 实现
  README.md            快速运行说明
  解决方案.md           设计与验收说明
  Dockerfile           容器化运行定义
  Makefile             演示及验证命令
  data/                原始响应、处理数据、状态库和运行结果
```

## 数据模型与可追溯性

规范记录以 `pubchem:<CID>` 为来源唯一主键，包含 `query`、IUPAC 名称、分子式、分子量和 `fetched_at`。原始响应保存为 `pubchem_<query>_<UTC时间>.json`，处理层使用 `processed/records.jsonl`，使数据可重放、审计和重新解析。

## 采集设计

`fetch` 通过关键词或 CID 调用 PubChem，获取 IUPAC 名称、分子式和分子量。纯数字查询会自动使用 CID 端点，其他查询使用化合物名称端点。

- 单请求超时 15 秒。
- 每次真实 HTTP 请求前等待至少 0.2 秒，避免短时间连续请求超过公开 API 的推荐频率。
- 网络、超时、JSON 解码失败最多 3 次重试，退避间隔为 1 秒、2 秒。
- 404 明确提示查询无结果；连续失败明确提示数据源不可达；响应 JSON 结构不符合预期时提示返回格式异常。
- `argparse` 将 `--source`、`--query`、`--output` 设为必传参数，缺失参数时直接输出命令用法和缺失项。
- 不符合预期的响应不会写入规范层。

真实 API 调用不传 `--mock` 即可执行。例如：

```powershell
python .\dataeng_cli.py fetch --source pubchem --query aspirin --output .\data
python .\dataeng_cli.py fetch --source pubchem --query 2244 --output .\data
```

若运行环境使用含自签名根证书的 HTTPS 代理，Python 可能报 `CERTIFICATE_VERIFY_FAILED`。该错误会被识别为数据源不可达；应在不经过该代理的网络中运行，或将企业根证书正确加入 Python 信任链，而不是关闭 TLS 证书验证。

## 增量与幂等

`sync` 使用 SQLite：`records(id PRIMARY KEY, payload, synced_at)` 保存最新规范记录；`state(key, value)` 保存 `watermark:pubchem`。同步时读取 watermark，传入的 `--since` 会优先作为本次请求水位；不存在的 CID 新增、payload 改变则更新、完全相同则跳过。成功后才写入当前 UTC watermark，因此重复运行不会产生重复记录。

比较业务内容时会排除 `fetched_at`。该字段仅代表本次采集时间，不能使内容未变化的记录误判为更新。同步输出 `inserted`、`updated`、`skipped` 三项计数，适合调度任务统计。

PubChem 名称属性端点没有服务端更新时间变更流，因此不伪造按时间过滤能力。生产场景应选用具备变更游标的端点，或维护实体清单并周期性抓取，对 payload 哈希检测变更。

## 质量校验

`validate` 扫描目录内 JSONL，生成 JSON 报告。

- 必填字段：`id`、`query`、`iupac_name`、`molecular_formula`、`molecular_weight`、`fetched_at`。
- Schema：ID 必须以 `pubchem:` 开头；分子量必须为数值。
- 通过条件：有记录、完整率不低于 95%、重复率为 0、无 schema 错误。
- 空目录、坏 JSON、非对象记录均被安全处理并导致不通过；`comment` 会基于完整率、重复数和 Schema 错误数输出对应原因。

例如，正常数据的 `comment` 会列出完整率、无重复、Schema 符合以及最终通过结论；存在重复 ID 或无效 JSON 时，`comment` 会给出重复数、Schema 错误数和修复建议，同时返回 `pass: false`。校验失败时进程退出码为 `2`，以便任务调度器识别失败。

## 演示命令


```powershell

python .\dataeng_cli.py fetch --source pubchem --query aspirin --output .\data --result-output .\data\fetch-result.json --log-file .\data\events.jsonl --mock

python .\dataeng_cli.py sync --source pubchem --query aspirin --since 2026-07-01 --state .\data\state.db --output .\data\sync-result.json --log-file .\data\events.jsonl --mock

python .\dataeng_cli.py sync --source pubchem --query aspirin --state .\data\state.db --log-file .\data\events.jsonl --mock

python .\dataeng_cli.py validate .\data\processed --format json --output .\data\quality-report.json --log-file .\data\events.jsonl
```

第二次同步预期 `skipped: 1`；质量校验预期 `pass: true`。各命令均支持 `--help` 查看参数说明。

## 命令输入与输出示例

以下命令均在项目根目录执行。使用 `--mock` 时不会访问网络，适合演示和验收；需要调用真实 PubChem API 时，只需删除 `--mock`。

### 1. 查看命令帮助

```powershell
python .\dataeng_cli.py --help
python .\dataeng_cli.py fetch --help
python .\dataeng_cli.py sync --help
python .\dataeng_cli.py validate --help
```

可看到三个必需的子命令：`fetch`、`sync`、`validate`，以及每个命令支持的参数。

### 2. fetch：按关键词采集

```powershell
python .\dataeng_cli.py fetch --source pubchem --query aspirin --output .\data --result-output .\data\fetch-result.json --log-file .\data\events.jsonl --mock
```

终端输出示例：

```json
{
  "raw_response": "data\\pubchem_aspirin_20260818T082306Z.json",
  "processed_records": "data\\processed\\records.jsonl",
  "records": 1
}
```

同时生成：

- `data/pubchem_aspirin_<UTC时间>.json`：未修改的 PubChem 原始响应。
- `data/processed/records.jsonl`：规范化后的处理数据。
- `data/fetch-result.json`：本次命令的结果摘要。

处理后的单条 JSONL 数据示例：

```json
{
  "id": "pubchem:2244",
  "source": "pubchem",
  "query": "aspirin",
  "iupac_name": "2-acetyloxybenzoic acid",
  "molecular_formula": "C9H8O4",
  "molecular_weight": 180.16,
  "fetched_at": "2026-08-18T08:23:06Z"
}
```

### 3. fetch：按 CID 查询

纯数字 `query` 自动使用 PubChem CID 查询端点：

```powershell
python .\dataeng_cli.py fetch --source pubchem --query 2244 --output .\data --mock
```

### 4. sync：首次同步与幂等重跑

首次同步可指定初始水位线：

```powershell
python .\dataeng_cli.py sync --source pubchem --query aspirin --since 2026-07-01 --state .\data\state.db --output .\data\sync-result.json --log-file .\data\events.jsonl --mock
```

首次同步输出示例：

```json
{
  "source": "pubchem",
  "requested_since": "2026-07-01",
  "watermark": "2026-08-18T08:23:06Z",
  "inserted": 1,
  "updated": 0,
  "skipped": 0,
  "state": ".\\data\\state.db"
}
```

对同一数据再次执行同步，不传 `--since` 时会自动读取 SQLite 中保存的 watermark：

```powershell
python .\dataeng_cli.py sync --source pubchem --query aspirin --state .\data\state.db --log-file .\data\events.jsonl --mock
```

幂等重跑输出示例：

```json
{
  "source": "pubchem",
  "requested_since": "2026-08-18T08:23:06Z",
  "watermark": "2026-08-18T08:25:00Z",
  "inserted": 0,
  "updated": 0,
  "skipped": 1,
  "state": ".\\data\\state.db"
}
```

### 5. validate：成功质量报告

```powershell
python .\dataeng_cli.py validate .\data\processed --format json --output .\data\quality-report.json --log-file .\data\events.jsonl
```

输出示例：

```json
{
  "total_records": 1,
  "completeness_rate": 1.0,
  "duplicate_rate": 0.0,
  "schema_errors": 0,
  "stale_records": 0,
  "pass": true,
  "comment": "必填字段完整率为 100.00%，符合不低于 95% 的阈值；未发现重复的来源唯一 ID；字段类型和格式均符合预定义 Schema；数据质量校验通过。"
}
```

### 6. validate：失败质量报告

当目录中有重复 ID 或无效 JSON 时，命令仍会生成结构化报告，但以退出码 `2` 表示质量不通过：

```json
{
  "total_records": 2,
  "completeness_rate": 1.0,
  "duplicate_rate": 0.5,
  "schema_errors": 1,
  "stale_records": 0,
  "pass": false,
  "comment": "必填字段完整率为 100.00%，符合不低于 95% 的阈值；发现 1 条重复的来源唯一 ID；发现 1 条字段类型、格式或 JSON 结构错误；数据质量校验未通过，建议修复上述问题后重新执行校验。"
}
```

### 7. 结构化日志示例

`--log-file .\data\events.jsonl` 生成每行一条 JSON 的日志文件，例如：

```json
{"timestamp":"2026-08-18T08:23:06Z","event":"fetch_completed","source":"pubchem","query":"aspirin","records":1,"mock":true}
{"timestamp":"2026-08-18T08:23:06Z","event":"sync_completed","source":"pubchem","inserted":1,"updated":0,"skipped":0,"mock":true}
{"timestamp":"2026-08-18T08:23:07Z","event":"validation_completed","total_records":1,"passed":true,"schema_errors":0,"duplicate_rate":0.0}
```

### 8. Docker 和 Makefile

```powershell
# Docker：构建并检查 CLI 入口
docker build -t dataeng-cli-demo .
docker run --rm dataeng-cli-demo --help

# Makefile：需要本机安装 make
make demo
make verify
```

## 生产化演进

- 由 Airflow、Dagster 或 CronJob 调度，并增加运行 ID、指标和结构化日志。
- 原始层迁移对象存储，规范层迁移 Parquet/Iceberg，SQLite 替换为集中式状态库。
- Source adapter 引入限流、熔断、监控和契约测试。
- Schema 版本化，并增加新鲜度、异常分布、跨来源冲突等规则。

## 已实现加分项

- `--mock` 模式可在网络不可用或 API 不可用时完整演示核心流程。
- `fetch --result-output` 保存采集结果摘要；`sync --output` 保存同步统计；`validate --output` 保存质量报告，均为 UTF-8 JSON 文件。
- 所有命令支持 `--log-file`，以 JSON Lines 格式记录 `timestamp`、`event`、来源、记录计数、同步结果或校验状态。日志写入标准错误输出，因此不会混入标准输出的命令结果 JSON。
- 附带 `Dockerfile`，可使用 `docker build -t dataeng-cli-demo .` 构建，并使用 `docker run --rm dataeng-cli-demo --help` 检查命令入口。
- 附带 `Makefile`：`make demo` 执行离线完整链路，`make verify` 执行语法检查和已有处理数据的质量校验。
